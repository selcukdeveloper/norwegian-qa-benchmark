from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]

RAW_NORQUAD = ROOT / "NorQuAD data" / "evaluation" / "all"
 
# Processed JSONL (generated by src/data/process_*.py).
PROCESSED = ROOT / "processed"
PROC_NORQUAD = PROCESSED / "norquad"
PROC_NOBOOLQ = PROCESSED / "noboolq"

# Outputs.
RESULTS = ROOT / "results"
FIGS = RESULTS / "figures"
METRICS = RESULTS / "metrics"
PREDS = RESULTS / "predictions"
LOGS = RESULTS / "logs"
CKPTS = RESULTS / "checkpoints"

# Tier cutoff: NorQuAD answer of <=5 whitespace tokens is "short", else "long".
SHORT_MAX_TOKENS = 5
SEED = 42

# Models
M_MBERT = "bert-base-multilingual-cased"          # multilingual transformer baseline
M_NB_BERT_BASE = "NbAiLab/nb-bert-base"           # Norwegian-specific transformer
M_NORMISTRAL_11B_THINKING = "norallm/normistral-11b-thinking"  # zero-shot generative reference
